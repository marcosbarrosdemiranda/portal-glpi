# Log — 2026-06-12 — Sessão 01 (inicial)

> ver [[index]]. Estado ao fim da sessão.

## O que foi feito

1. **Estudo da API Sólides/Tangerino** e guia de referência salvo (`tangerino-api-reference.md`). Ver [[Integracao-API-Solides]].
2. **Extração inicial** via token: 6 lojas (com CNPJ, via `/companies`) e ~218 colaboradores ativos.
3. **Decisão de arquitetura**: espelho local + MySQL ([[ADR-002-Sync-Local-vs-Tempo-Real]], [[ADR-001-Banco-MySQL]], [[ADR-003-Stack-NodeJS]]).
4. **MariaDB 12.3** instalado (serviço Windows), database `ponto_gmais`, usuário `ponto`.
5. **tangerino-sync** (Node + mysql2): [[Modelo-de-Dados|16 tabelas]], modos full/inc/rápido, agendamento 10 min / 1 h. Ver [[Sincronizacao-de-Dados]].
6. **dashboard** (Express + JWT + bcrypt): API REST, [[Seguranca-e-Perfis|perfis ADMIN/RH/GESTOR]], [[Dashboards-e-Relatorios|PWA]] com 5 abas e filtros.
7. **Migração SQLite → MySQL** completa, com correção da pegadinha `namedPlaceholders`.
8. **Verificação** com dados reais: 218 ativos, 1.305 h extras no mês; escopo de GESTOR e bloqueio de `/usuarios` testados.
9. **Documentação** (este vault Obsidian).

## Carga full (referência)

`lojas 7 · setores 24 · cargos 150 · escalas 46 · colaboradores 974 · ajustes 37.353 · batidas 9.346 · resumo 7.441 · banco_horas 218` em ~495 s.

## Continuação (tarde/noite de 12/06)

10. **BI de Horas Extras** réplica do Power BI (`bi.html`): 3 visões (Matriz, Lojas com pizza, Departamentos com barras) — ver [[Dashboards-e-Relatorios]]. Calibrado vs Power BI (loja 001 = 215:51 vs 215:59).
11. **Redesign do BI**: logo removido (cliente achou feio), paleta refinada (vinho + acento #e11d2e), `color-scheme: only light`.
12. **Abertura local**: `Iniciar Painel.cmd`, atalho no Desktop, auto-start no logon. **Kit de deploy** em `deploy\` (Linux systemd+Caddy/HTTPS e Windows). Backup diário 22h.
13. **Fuso definido em UTC-4** (`America/Campo_Grande`, Campo Grande/MS = 1h a menos que Brasília) — confirmado com batida real. Revertido o -5 anterior.
14. **Bug corrigido**: tarefas do agendador com caminho com espaços falhavam (0x80070002) → refeitas com `Set-ScheduledTask`. Sync rápido (10min), horário (1h) e backup validados com resultado 0.
15. **Banco de horas removido** (empresa não usa): aba, endpoint e coleta `/hoursBalance` desativados — sync ficou mais rápido.

## Decisões importantes

- Horas extras **nunca recalculadas** (usar `resumo_diario`).
- Sem Excel (removido a pedido do usuário).
- Datas tratadas em JS no fuso `America/Campo_Grande`, banco independente de fuso.

## Pendências / onde retomar

- **Acesso fora da rede + HTTPS** (executar o kit de deploy no servidor definitivo). Ver [[Backlog]] (Fase 2).
- **APK**: gerar via TWA depois do HTTPS (hoje funciona como PWA "adicionar à tela inicial").
- Criar usuários GESTOR das demais lojas (003/010/030/031/101).
- Trocar senhas padrão e mover segredos antes de produção.

## Credenciais (provisórias)

Ver [[CONTRIBUTING#Segredos e credenciais]].
